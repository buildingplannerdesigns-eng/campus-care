import type { ExternalResource } from "@/types";

/**
 * Preferred external links, as supplied by the client
 * (see: Links to preferred links.docx).
 * Surfaced in the site footer and on the About page.
 */
export const externalResources: ExternalResource[] = [];
